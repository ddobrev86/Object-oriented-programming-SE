#pragma once

unsigned charToDigit(char);

char digitToChar(unsigned);

unsigned toPower(unsigned, unsigned);

size_t digitCount(unsigned);

char* numberToString(unsigned);

unsigned stringToNumber(const char*);

void reverseString(char*);
